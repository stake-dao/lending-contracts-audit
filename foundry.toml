[profile.default]
src = "src"
out = "out"
libs = ["lib", "dependencies"]
solc = "0.8.28"
evm_version = "cancun"
optimizer = true
optimizer_runs = 200
verbosity = 2

[profile.default.fuzz]
runs = 50

[profile.default.invariant]
depth = 10
fail_on_revert = true
